# 2026-08-07 悦游部署路径与发布工作流同步

## 目标

根据服务器目录调整，统一阅游服务端的生产运行目录和发布操作说明，避免将部署产物误传到其他应用目录。

## 变更点

- 明确 Go 服务端源码目录为仓库 `server/`。
- 明确生产运行包目录为 `/www/wwwroot/yueyou/`。
- 明确 systemd 托管单元为 `yueyou.service`，环境文件为 `/www/wwwroot/yueyou/.env`。
- 在发布守卫、环境配置、任务收口和技能指南中补齐上传、重启、状态检查及公网接口验证步骤。
- 不纳入 `burudushu`、`yunxibakebot` 和 `server-monitor` 的目录说明。

## 验证结果

- 已通过全文检索确认本次工作流文档不再引用其他项目的阅游部署目录。
- 本次仅修改 Markdown 与规则文件，未触及 Dart、Go 业务代码，未运行 Flutter 或 Go 编译门禁。

## 后续风险

- `<生产服务器>` 仍需由实际运维环境替换为当前 SSH 主机别名或地址；文档不保存密钥。
- 本次未执行远程上传、重启或公网探活，不能据此宣称生产部署已完成。
