# 20260813 阶段 0 落地差距评估

## 背景

项目此前获得一份「阶段 0 审批方案」（四周、34 人日、外部现金上限 20,000 元），
方案声称工程侧已具备若干前置产物（PROD-00、PERF-0-A、可点击手机 HTML 原型、
Xiaoyo 候选概念图），并要求在四周内完成真实用户访谈、真实支付实验、真机性能
基线与 IP-0 资产验收。用户判断"目前工作区的内容根本落地不了"。

本报告对工作区真实状态进行逐项核查，区分：

- **当前事实**：由仓库文件、代码、测试与文档可验证的状态。
- **建议目标**：详设中推荐但尚未验证的架构或验收指标。
- **商业假设**：必须由真实用户行为与真实支付才能证明的结论。

本报告不修改业务代码，只做证据核查与差距评估。

## 核查方法与范围

核查基于当前检出分支 `codex/prod-03-a-tts-guard`（本地与 `origin` 同名分支一致），
核对以下来源：

- `DevelopmentPlan/20260713_性能架构原型与改进报告.md`（详设主文档）
- `DevelopmentPlan/20260714_IP-0资产治理与性能门执行.md`
- `DevelopmentPlan/20260715_浏览器验收与阶段门收口.md`
- `docs/product/assets/20260714_xiaoyo_v2_ip0_manifest.json`
- `docs/product/20260714_原型合同检查.md`
- `docs/performance/baselines/README.md`
- `lib/`、`scripts/`、`docs/` 下的实际源码、脚本与产物文件
- 本机 `adb devices`、git 分支与提交记录

## 结论一：文档没有虚报，工程侧产物真实存在

以下"声称已完成"的工程产物经文件系统核对全部存在：

| 声称产物 | 实际文件 | 状态 |
| --- | --- | --- |
| PROD-00 功能开关 | `lib/core/config/feature_flags.dart` | 存在 |
| PERF-0-A 帧统计框架 | `lib/core/performance/frame_timing_summary.dart` | 存在 |
| 三根导航壳 | `lib/features/app_shell/` | 存在 |
| 七态听读首页 | `lib/features/reader/presentation/screens/reading_home_screen.dart` | 存在 |
| Xiaoyo 领域/Provider/桥 | `lib/features/xiaoyo/` | 存在 |
| 作者听校本地领域 | `lib/features/author/` | 存在 |
| 手机 HTML 原型 | `docs/product/20260713_阅游产品改进手机原型.html` | 存在 |
| 原型结构/交互合同检查 | `scripts/check_product_prototype.py`、`scripts/test_product_prototype.py` | 存在 |
| 真机采集与基线比较工具 | `scripts/performance/run_android_profile.ps1`、`compare_baselines.dart` | 存在 |
| IP-0 清单（candidate） | `docs/product/assets/20260714_xiaoyo_v2_ip0_manifest.json` | 存在，六类证据均 false |
| 商业证据计算器 | `scripts/commerce_experiment_evaluator.dart`、`evaluate_commerce_experiment.dart` | 存在 |
| Xiaoyo 候选概念图 | `docs/product/assets/20260713_xiaoyo_v2_concept_candidate.png` | 存在 |
| 社区 Rive（仅兼容核对） | `assets/rive/xiaoyo.riv` | 存在，禁止商业使用 |

git 提交链（`9833074` 至更早）与上述产物一一对应，最近一次为 07-15 的浏览器
验收收口与部署文档同步。

## 结论二：文档声称"未完成"的也确实未完成

| 声称未完成项 | 实际状态 | 核对依据 |
| --- | --- | --- |
| 正式 commerce 模块 | 不存在 | `lib/features/commerce/` 不存在 |
| 原创 Rive 母版 | 不存在 | `assets/rive/xiaoyo_v2_base.riv` 不存在 |
| IP-0 六类证据 | 全部未提供 | manifest 中 six 项 `complete: false` |
| 真机性能基线（G0） | 未执行 | 本机 `adb devices` 为空，无设备连接 |
| 真实支付渠道/退款/台账 | 未提供 | 详设与 07-15 收口文档均标 `[ ]` |
| 用户访谈/体验/访问者数据 | 未提供 | 详设与收口文档均标 `[ ]` |
| 作者听校真实样本 | 未提供 | 详设第 43 节标 `[ ]` |

关键点：**文档对"已完成"与"未完成"的区分是诚实的**。07-13 详设开篇即声明
"不代表功能已实现"，07-15 收口文档也明确写出商业实验状态为"未启动"、IP-0 为
`candidate`、G0 未签署。

## 结论三：用户"落地不了"的判断成立，卡在结构层面

把阶段 0 交付物按"代码能否独立完成"切分：

| 阶段 0 交付物 | 工作区现状 | 本地能否闭环 |
| --- | --- | --- |
| 可点击手机 HTML | 完成 + 浏览器验收 | 已完成 |
| 成长 + 印记 + 荣誉 | 领域代码已实现 | 已完成 |
| PROD-00 开关/回滚 | 已实现 | 已完成 |
| 免费/付费书境规则 | 仅只读预览 | 部分 |
| PERF-0 真机基线 | 工具齐备但无设备 | 需要一台 Android 真机 |
| 24 次访谈 / 40 次有效体验 | 无用户渠道 | 需要真实用户 |
| 240–300 合格访问者 | 无流量渠道 | 需要用户来源 |
| Local Pro 订金 / 书境包全款 | 无合规商户 + 退款能力 | 需要商户资质 |
| 作者听校 49 元实验 | 仅本地仓储 | 需要作者样本 |
| Xiaoyo 四视图/表情/材质/动作 | 仅 1 张候选图 | 需要设计师 |
| 原创近似检查 + 授权链 | 仅有门禁清单 | 需要资产/法务方 |
| 基础 Rive 动效样例 | 无原创 .riv | 需要动画师 |

由此得到结构性判断：

1. **工程侧确实落地了**：代码、测试、门禁、契约检查真实存在且自洽，全量测试
   与静态检查按文档记录均通过。
2. **阶段 0 的核心是商业验证，不是工程验证**：而商业验证的关键交付物（真实用户、
   真实支付、真机性能、原创 IP 资产）没有一项能由代码仓库独立完成，全部依赖
   仓库之外的外部资源。
3. **工作区把"工具"做齐了，但工具不等于数据、不等于结果**：`commerce_experiment_
   evaluator.dart` 在没有真实台账输入时输出永远是 `insufficientEvidence`；
   `run_android_profile.ps1` 在没有设备时无法产生任何基线；`check_xiaoyo_ip0.dart`
   在六类证据缺位时始终报告未就绪。工具越严谨，越说明"数据尚未进入"。
4. **方案默认存在执行主体**：34 人日、2 万现金、四周，隐含团队、预算与审批流程；
   从工作区只能看到工程产出，看不到任何外部资源投入迹象。阶段 0 的执行前置条件
   （人、钱、资质、设备、流量）全部在工作区之外——这正是"落地不了"的准确含义。

## 结论四：如果推进，可行的拆法

阶段 0 按"本机 + 最低外部依赖"重排优先级：

1. **可立即做**（唯一"一台设备就能闭环"的硬门）：借/购一台 Android 真机
   （千元档即可），按 `docs/performance/baselines/README.md` 执行两轮同设备采集，
   用 `compare_baselines.dart` 生成脱敏对比报告，完成 G0 签署。
2. **可小成本做**：把 24 次访谈压缩到 5–10 位真实陌生人（非朋友），只验证
   "听读 + 2048" 是否有真实吸引力，替代全量商业实验。
3. **必须等外部资源**：真实支付实验（商户资质 + 退款能力）、IP-0 六类资产
   （设计师/动画师/法务）、作者听校（作者样本）。三者在资源到位前无法启动，
   继续堆文档不会改变其状态。

## 决策建议

- 阶段 0 的"工程前置"部分已达标，可以作为谈判基础，但不应被描述为"商业验证
  已完成"。
- 四周 34 人日的原始方案在无外部资源投入的情况下不可执行；建议先按结论四的
  优先级 1 与 2 落地最小闭环，用真实行为数据决定是否投入商户资质与 IP 资产。
- 保持"免费核心路径 + 本地数据 + 开关默认关闭"的既有边界，直至真实支付门
  与 IP-0 门通过。

## 遗留事项

- 当前 git 工作区存在未提交文件（copyright 文档生成脚本与 PDF、先前下载的
  `main.js`/`rendered_page.html` 等），与本报告无关，未做清理或提交，待用户决定。
- 本报告仅为证据核查与差距评估，未修改任何业务代码。

---

## 追加：P0 根目录治理执行记录（2026-08-13 同日）

在用户决定后执行仓库根目录清理，消除"乱糟糟"的部分来源：

### 已删除（根目录测试残留与临时下载物，均未跟踪）

| 文件 | 性质 |
| --- | --- |
| `tts_1784024760066550.mp3`、`tts_1784024764978504.mp3` | TTS 测试残留 |
| `fail.log`、`flutter_01.log`、`fulltest.log`、`test_output.log` | 测试日志 |
| `main.js`、`main2.js`、`rendered_page.html`、`share_page.html`、`jsfiles/` | Devin 分享页下载物 |

### 已归档至 `docs/historical/`（消除"AI 转型 / 阅读优先"双愿景混存）

| 文件 | 处理 |
| --- | --- |
| `yueyou-ai-transformation-plan.md` | `git mv`（保留历史） |
| `yueyou-ai-prototype.html` | `git mv`（保留历史） |
| `基于Riverpod与Dart3彻底重写TTS音频流状态机.md` | 移动（已完成的历史任务书） |
| `docs/historical/README.md` | 新建归档说明（清单 + 规则） |

### 保留未动

- `贡献指南.md`：仍被 `README.md` 引用，保留在根目录。
- `docs/copyright/` 版权材料、`server_py/`、`task.md` 等已跟踪散落 md、全部业务代码：属后续 P1/P2 决策范围。

### 验证

- 根目录已无 mp3 / log / js / html 残留。
- `yueyou-ai-*` 不再被任何文档引用，无断链。
- 仅文档与归档变更，未触碰业务代码，工程门禁不受影响。

---

## 追加：P1/P2 仓库结构收口执行记录（2026-08-13 同日）

P0 清理后继续收口规则文件、散落文档、实验后端与死分支：

### 规则文件统一（消除三份内容漂移）

- `CLAUDE.md` 为唯一权威全文：升级"服务端部署"段落为 scp + `systemctl is-active` 版本。
- `AGENTS.md` 改为兼容入口指针（其他工具读取时指向 `CLAUDE.md`）。
- 删除 `.windsurf/rules/AGENT.md`（重复副本，内容已由 `CLAUDE.md` + `.agents/skills/` 覆盖）。

### 根目录散落文档归档（均 `git mv` 保留历史）

| 文件 | 去向 | 理由 |
| --- | --- | --- |
| `task.md` | `docs/historical/` | 已完成的历史任务清单 |
| `STORE_LISTING.md` | `docs/product/` | 商店上架文案（发版活跃文档） |
| `TTS_FLOW.md` | `docs/` | 当前 TTS 架构流程图 |
| `yueyouflowchart.md` | `docs/` | 应用整体流程图 |

同步更新 `贡献指南.md` 中对 `task.md` 的引用。

### `server_py/` 处置

- FastAPI/edge-tts 实验后端（AI 转型方向残留，未跟踪、不在 CI、未部署）。
- 经用户确认移出仓库备份至 `D:\Project\_yueyou_archive\server_py\`，代码不丢失（可逆）。

### 分支清理（删除前已用归档标签保护历史）

| 分支 | 处置 |
| --- | --- |
| `cascade/*` ×3（2026-03/05 自动快照） | 删除本地，含关联孤儿 worktree |
| `yueyou_test`（本地） | 删除（HEAD 祖先，无独有提交） |
| `origin/master`（初始化提交） | 删除远程，标签 `archive/origin-master-e18df48` 保护 |
| `origin/yueyou_old_test`（旧结构历史） | 删除远程，标签 `archive/origin-yueyou-old-test-af43d59` 保护 |
| `origin/yueyou_test`（304 个 M4 活跃提交） | 保留 |
| `codex/prod-03-a-tts-guard`（当前） | 保留 |

### 收口结果

- 分支全景收敛为：本地 1 条（`codex/prod-03-a-tts-guard`）+ 远程 2 条（`yueyou_test`、`codex/prod-03-a-tts-guard`）。
- 根目录无散落业务文档；规则文件仅 `CLAUDE.md` 一份全文权威。
- 未动：`docs/copyright/` 版权材料及生成脚本、`.codex/`、`.playwright-cli/`、
  `.superpowers/`、`DevelopmentPlan/devplan_list.txt`（不属于本次收口范围）。
