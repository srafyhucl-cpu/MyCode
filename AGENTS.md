# 阅游 (YueYou) - 开发规范入口

本项目的**唯一权威开发规范**为 [`CLAUDE.md`](CLAUDE.md)（极客开发手册）。

请完整阅读 `CLAUDE.md`,其中包含:

- 技术栈与 Feature-Driven Clean Architecture 约束
- 开发红线(副作用禁令、GPU 动画、重绘隔离、零硬编码、异步解析、数据隐私、零警告)
- 职责过载信号与拆分评估规则
- TTS 云端通信契约
- 日志使用规范
- 行为提示(任务单 / 日志 / README / 提交 / 推送 / 收口顺序)
- 质量门禁与自动化检查清单

专项细节规则位于 `.agents/skills/`(架构守卫、文件体量、代码质量、配置常量、测试与 CI、TTS 音频、UI 性能、文档编码、Domain 纯逻辑、发版就绪、迁移约束、任务管理)。

> 本文件为兼容入口,避免多份规则副本内容漂移;规范内容一律以 `CLAUDE.md` 为准。
